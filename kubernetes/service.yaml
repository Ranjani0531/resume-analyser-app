apiVersion: v1
kind: Service
metadata:
  name: resume-analyser-service
spec:
  type: NodePort  

  selector:
    app: analyser-app
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30007
